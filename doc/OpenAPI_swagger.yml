openapi: 3.0.0
info:
  title: Participium API
  description: API for the Participium application
  version: 0.1.0

servers:
  - url: http://localhost:4000/api
    description: Development server

paths:
  /auth/session:
    # login endpoint
    post:
      tags: [Authentication]
      summary: Login
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginRequest'
      responses:
        '200':
          description: OK - Successfully authenticated
          headers:
            Set-Cookie:
              description: Authentication cookie
              schema:
                type: string
                example: "authToken=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ1c2VySWQiOjEsInVzZXJuYW1lIjoibXJvc3NpIiwiaWF0IjoxNjk5MDAwMDAwLCJleHAiOjE2OTkwODY0MDB9"
            Location:
                description: URL of the reserved area for submitting reports
                schema:
                  type: string
                  example: /reports
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
        '400':
          description: Bad Request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized - Invalid credentials
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: "Authentication Error"
                message: "Invalid username or password"

    # verify authentication endpoint
    get:
      tags: [Authentication]
      summary: Verify authentication
      description: Check if the user is currently authenticated
      security:
        - cookieAuth: []
      responses:
        '200':
          description: OK - User is authenticated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
        '401':
          description: Unauthorized - User is not authenticated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: "Authentication Error"
                message: "Session is invalid or has expired"
    delete:
      tags: [Authentication]
      summary: Logout
      description: Logout the authenticated user
      security:
        - cookieAuth: []
      responses:
        '204':
          description: No content - Successfully logged out
        '401':
          description: Unauthorized - User is not authenticated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: "Authentication Error"
                message: "You must be logged in to logout"
  /reports:
    # create report endpoint
    post:
      tags: [Reports]
      summary: Create a new report
      description: Submit a new report (authenticated users only)
      security:
        - cookieAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ReportRequest'
      responses:
        '201':
          description: Created - Report successfully submitted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Report'
        '400':
          description: Bad Request - Invalid input or missing required fields
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized - User is not authenticated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: "Authentication Error"
                message: "You must be logged in to create a report"
    get:
      tags: [Reports]
      summary: List all reports
      description: >
        Returns the list of all reports. 
      security:
        - cookieAuth: []
      parameters:
        - in: query
          name: status
          schema:
            type: string
            enum:
              - ASSIGNED
          description: Filter reports by status
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Report'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '403':
          description: Forbidden - You don't have admin or municipality permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /reports/{id}:
    get:
      tags: [Reports]
      summary: Get report by ID
      description: Retrieve a specific report by its ID (public)
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
            format: int64
          description: Report ID
      responses:
        '200':
          description: OK - Report found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Report'
        '400':
          description: Bad Request - Invalid ID
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Not Found - Report not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

    post:
      tags: [Reports]
      summary: Change report status
      description: >
        Change the status of a report by ID. For rejection a motivation is required.
        Municipal public officer can also change the category of the report.
        **Requires role: MUNICIPALITY, EXTERNAL_MAINTAINER**.
      security:
        - cookieAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
            format: int64
          description: Report ID
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - status
              properties:
                status:
                  type: string
                  enum:
                    - ASSIGNED
                    - REJECTED
                  example: REJECTED
                motivation:
                  type: string
                  example: "The report is not valid because photos are blurry."
                category:
                  $ref: '#/components/schemas/ReportCategory'
      responses:
        '204':
          description: OK - Report successfully validated
        '400':
          description: Bad Request - Invalid ID
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: "Bad Request"
                message: "Invalid report ID"
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '403':
          description: Forbidden - You don't have municipality permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Not Found - Report not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: "Not Found"
                message: "Report not found"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /reports/municipality-users/{municipalityUserId}:
    get:
      tags: [Reports]
      summary: Get reports by municipality user ID
      description: >
        Get reports by municipality user ID.
        **Requires role: MUNICIPALITY**.
      security:
        - cookieAuth: []
      parameters:
        - in: path
          name: municipalityUserId
          required: true
          schema:
            type: integer
            format: int64
          description: Municipality user ID
      responses:
        '200':
          description: OK - Reports successfully retrieved
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Report'
        '400':
          description: Bad Request 
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: "Bad Request - Validation error"
                message: "Invalid input data"
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '403':
          description: Forbidden - You don't have municipality permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Not Found - Role not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: "Not Found"
                message: "Role not found"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /reports/{id}/comments:
    post:
      tags: [Reports]
      summary: Add internal comment to a report
      description: >
        Add an internal comment to a report by ID.
        **Requires role: MUNICIPALITY_USER or EXTERNAL_MAINTAINER**.
        Comments are not visible to citizens.
      security:
        - cookieAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
            format: int64
          description: Report ID
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CommentRequest'
      responses:
        '201':
          description: Created - Comment successfully added
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Comment'
        '400':
          description: Bad Request - Invalid report ID or input data
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: "Bad Request"
                message: "Invalid report ID"
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '403':
          description: Forbidden - You don't have municipality or external maintainer permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Not Found - Report not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: "Not Found"
                message: "Report not found"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

    get:
      tags: [Reports]
      summary: Get internal comments for a report
      description: >
        Get internal comments for a report by ID.
        **Requires role: MUNICIPALITY_USER or EXTERNAL_MAINTAINER**.
        Comments are not visible to citizens.
      security:
        - cookieAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
            format: int64
          description: Report ID
      responses:
        '200':
          description: OK - Comments successfully retrieved
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Comment'
        '400':
          description: Bad Request - Invalid report ID
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: "Bad Request"
                message: "Invalid report ID"
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '403':
          description: Forbidden - You don't have municipality or external maintainer permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Not Found - Report not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: "Not Found"
                message: "Report not found"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'


  /users:
    # registration endpoint
    post:
      tags: [Users]
      summary: Registration
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserRegistration'
      responses:
        '201':
          description: Created
          headers:
            Set-Cookie:
              description: Authentication cookie
              schema:
                type: string
                example: "authToken=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ1c2VySWQiOjEsInVzZXJuYW1lIjoibXJvc3NpIiwiaWF0IjoxNjk5MDAwMDAwLCJleHAiOjE2OTkwODY0MDB9"
            Location:
                description: URL of the reserved area for submitting reports
                schema:
                  type: string
                  example: /reports
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
        '400':
          description: Bad Request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '409':
          description: Conflict
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: "Conflict Error"
                message: "Username or email already exists"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    get:
      tags: [Users]
      summary: List all users
      description: >
        Returns the list of all users (citizens and municipality staff).
        **Requires role: ADMIN**.
      security:
        - cookieAuth: []
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/AdminUser'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '403':
          description: Forbidden - You don't have admin permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    patch:
      tags: [Users]
      summary: Account configuration
      description: >
        Require role: CITIZEN.
      security:
        - cookieAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/UserConfiguration'
      responses:
        '200':
          description: OK
        '400':
          description: Bad Request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /users/{id}:
    get:
      tags: [Users]
      summary: Get user by ID
      description: >
        Get user details by ID.
        **Requires role: ADMIN**.
      security:
        - cookieAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
            format: int64
          description: User ID
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminUser'
        '400':
          description: Bad Request - Invalid ID
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: "Bad Request"
                message: "Invalid user ID"
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '403':
          description: Forbidden - You don't have admin permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Not Found - User not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: "Not Found"
                message: "User not found"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    delete:
      tags: [Users]
      summary: Delete user
      description: >
        Delete a user by ID.
        **Requires role: ADMIN**.
      security:
        - cookieAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
            format: int64
          description: User ID
      responses:
        '204':
          description: No Content - Successfully deleted
        '400':
          description: Bad Request - Invalid ID
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: "Bad Request"
                message: "Invalid user ID"
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '403':
          description: Forbidden - You don't have admin permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Not Found - User not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: "Not Found"
                message: "User not found"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /users/municipality-users:
    post:
      tags: [Users]
      summary: Create a municipality user
      description: >
        Create an account for a municipality user.
        **Requires role: ADMIN**.
      security:
        - cookieAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MunicipalityUserCreateRequest'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminUser'
        '400':
          description: Bad Request - Missing or invalid data
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: "Bad Request"
                message: "Missing required fields: email, username, firstName, lastName, password, municipality_role_id"
        '401':
          description: Unauthorized - Not authenticated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '403':
          description: Forbidden - admin permissions required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '409':
          description: Conflict - Email or username already in use
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: "Conflict Error"
                message: "Email is already in use"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    get:
      tags: [Users]
      summary: List municipality users
      description: >
        Returns the municipality users.
        **Requires role: ADMIN**.
      security:
        - cookieAuth: []
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/MunicipalityUser'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '403':
          description: Forbidden - You don't have admin permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /users/municipality-users/roles:
    get:
      tags: [Users]
      summary: List municipality roles
      description: >
        Returns the available roles for municipality users.
        **Requires role: ADMIN**.
      security:
        - cookieAuth: []
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/MunicipalityRole'
              examples:
                example:
                  value:
                    - id: 1
                      name: "OPERATOR"
                    - id: 2
                      name: "VALIDATOR"
                    - id: 3
                      name: "SUPERVISOR"
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '403':
          description: Forbidden - You don't have admin permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /users/external-users:
    post:
      tags: [Users]
      summary: Create a external company user for maintenance
      description: >
        Create an account for a external company.
        **Requires role: ADMIN**.
      security:
        - cookieAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ExternalUserCreateRequest'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminUser'
        '400':
          description: Bad Request - Missing or invalid data
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: "Bad Request"
                message: "Missing required fields: email, username, companyName, password, category"
        '401':
          description: Unauthorized - Not authenticated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '403':
          description: Forbidden - admin permissions required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '409':
          description: Conflict - Email or username already in use
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: "Conflict Error"
                message: "Email is already in use"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    get:
      tags: [Users]
      summary: List external company accounts
      description: >
        Returns the external company accounts.
        **Requires role: ADMIN**.
      security:
        - cookieAuth: []
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/AdminUser'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '403':
          description: Forbidden - You don't have admin permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      
  /reports/{report_id}/external-maintainers:
   post:
      tags: [Reports]
      summary: Assign a report in ASSIGNED status to an external maintainer
      description: >
        Assign a report to an external maintainer. Can be done only by technical municipality users.
        **Requires role: MUNICIPALITY**.
      security:
        - cookieAuth: []
      parameters:
        - in: path
          name: report_id
          required: true
          schema:
            type: integer
            format: int64
          description: Report ID
      responses:
        '204':
          description: OK - Report successfully assigned
        '400':
          description: Bad Request - Invalid ID
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: "Bad Request"
                message: "Invalid report ID"
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '403':
          description: Forbidden - You don't have municipality permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Not Found - Report not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: "Not Found"
                message: "Report not found"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /reports/external-maintainers/{id}:
    get:
      tags: [Reports]
      summary: Get reports assigned to an external maintainer
      description: >
        Get reports assigned to an external maintainer by ID.
        **Requires role: MUNICIPALITY, EXTERNAL_MAINTAINER**.
      security:
        - cookieAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
            format: int64
          description: External maintainer user ID
      responses:
        '200':
          description: OK - Reports successfully retrieved
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Report'
        '400':
          description: Bad Request 
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: "Bad Request - Validation error"
                message: "Invalid input data"
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '403':
          description: Forbidden - You don't have permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Not Found - External maintainer not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: "Not Found"
                message: "External maintainer not found"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'


components:
  schemas:    
    UserRegistration:
      type: object
      required:
        - firstName
        - lastName
        - username
        - email
        - password
      properties:
        firstName:
          type: string
          example: "Mario"
        lastName:
          type: string
          example: "Rossi"
        username:
          type: string
          example: "mrossi"
        email:
          type: string
          format: email
          example: "mario.rossi@example.com"
        password:
          type: string
          format: password
          example: "P@ssw0rd!"

    UserConfiguration:
      type: object
      properties:
        photo:
          type: string
          format: binary
          description: "JPEG, JPG or PNG image file"
        telegramUsername:
          type: string
          pattern: '^@[a-zA-Z0-9_]{5,32}$'
          example: "@Rossi"
        notificationsEnabled:
          type: string
          example: true
          description: Boolean

    User:
      type: object 
      properties:
        firstName:
          type: string
          example: "Mario"
        lastName:
          type: string
          example: "Rossi"
        username:
          type: string
          example: "mrossi"
        role:
          $ref: '#/components/schemas/Role'
        telegramUsername:
          type: string
          example: "@Rossi"
        notificationsEnabled:
          type: boolean
          example: true
        profilePhoto:
          type: string
          format: uri
          example: "https://example.com/photos/mrossi_profile.jpg"

    LoginRequest:
      type: object
      required:
        - identifier
        - password
      properties:
        identifier:
          type: string
          description: Username or email
          example: "mrossi"
        password:
          type: string
          format: password
          example: "P@ssw0rd!"
        role:
          $ref: '#/components/schemas/Role'
          description: Optional. If not provided, the system will search across all user types (citizen, municipality, admin, external maintainer) 
          
    ReportCategory:
      type: string
      enum:
        - Water Supply – Drinking Water
        - Architectural Barriers
        - Sewer System
        - Public Lighting
        - Waste
        - Road Signs and Traffic Lights
        - Roads and Urban Furnishings
        - Public Green Areas and Playgrounds
        - Other
        - WATER_SUPPLY_DRINKING_WATER
        - ARCHITECTURAL_BARRIERS
        - SEWER_SYSTEM
        - PUBLIC_LIGHTING
        - WASTE
        - ROAD_SIGNS_TRAFFIC_LIGHTS
        - ROADS_URBAN_FURNISHINGS
        - PUBLIC_GREEN_AREAS_PLAYGROUNDS
        - OTHER
      example: "Public Lighting"

    ReportRequest:
      type: object
      required:
        - title
        - description
        - category
        - photos
        - latitude
        - longitude
      properties:
        title:
          type: string
          example: "Broken street light"
        description:
          type: string
          example: "The street light on Via Roma has been broken for a week"
        anonymous:
          type: boolean
          example: false
        category:
          $ref: '#/components/schemas/ReportCategory'
        photos:
          type: array
          items:
            type: string
            format: binary
          minItems: 1
          maxItems: 3
          description: Array of image files to upload (multipart/form-data)
        latitude:
          type: number
          format: float
          example: 45.4642
        longitude:
          type: number
          format: float 
          example: 9.1900

    Report:
      type: object
      properties:
        id:
          type: integer
          format: int64
          example: 1
        title:
          type: string
          example: "Broken street light"
        description:
          type: string
          example: "The street light on Via Roma has been broken for a week"
        anonymous:
          type: boolean
          example: false
        category:
          $ref: '#/components/schemas/ReportCategory'
        photos:
          type: array
          items:
            type: string
            format: uri
          minItems: 1
          maxItems: 3
          example: 
            - "https://example.com/photos/report1_photo1.jpg"
            - "https://example.com/photos/report1_photo2.jpg"
        createdAt:
          type: string
          format: date-time
          example: "2025-11-04T14:30:00Z"
        latitude:
          type: number
          format: float
          example: 45.4642
        longitude:
          type: number
          format: float 
          example: 9.1900
        status:
          type: string
          enum:
            - PENDING_APPROVAL
            - ASSIGNED
            - IN_PROGRESS
            - SUSPENDED
            - REJECTED
            - RESOLVED
          example: "ASSIGNED"

    MunicipalityUserCreateRequest:
      type: object
      required:
        - email
        - username
        - firstName
        - lastName
        - password
        - municipality_role_id
      properties:
        email:
          type: string
          format: email
          example: "tecnico.comune@example.it"
        username:
          type: string
          example: "tcomune"
        firstName:
          type: string
          example: "Teodoro"
        lastName:
          type: string
          example: "Comune"
        password:
          type: string
          format: password
          example: "Str0ngP@ss!"
        municipality_role_id:
          type: integer
          format: int64
          example: 2
          description: Municipality role ID (see /users/municipality-users/roles GET endpoint)

    MunicipalityUser:
      type: object
      properties:
        email:
          type: string
          format: email
          example: "tecnico.comune@example.it"
        username:
          type: string
          example: "tcomune"
        firstName:
          type: string
          example: "Teodoro"
        lastName:
          type: string
          example: "Comune"
        municipality_role_id:
          type: integer
          format: int64
          example: 2
          description: Municipality role ID
        municipality_role_name:
          type: string
          example: "VALIDATOR"
          description: Municipality role name

    MunicipalityRole:
      type: object
      properties:
        id:
          type: integer
          format: int64
          example: 2
        name:
          type: string
          example: "VALIDATOR"

    AdminUser:
      type: object
      description: Admin view of a user
      properties:
        id:
          type: integer
          format: int64
          example: 15
        firstName:
          type: string
          example: "Mario"
        lastName:
          type: string
          example: "Rossi"
        companyName:
          type: string
          example: "Enel X"
        maintenanceCategory:
          $ref: '#/components/schemas/ReportCategory'
        username:
          type: string
          example: "mrossi"
        email:
          type: string
          format: email
          example: "mario.rossi@example.com"
        role:
          $ref: '#/components/schemas/Role'
        municipality_role_id:
          type: integer
          format: int64
          nullable: true
          example: 1
        municipality_role_name:
          type: string
          nullable: true
          example: "OPERATOR"
        createdAt:
          type: string
          format: date-time
          example: "2025-11-04T14:30:00Z"
    
    Role:
      type: string
      enum:
          - ADMIN
          - CITIZEN
          - MUNICIPALITY
          - EXTERNAL_MAINTAINER
      example: "ADMIN"

    ExternalUserCreateRequest:
      description: External company user related to a specific maintenance category
      type: object
      required:
        - email
        - username
        - companyName
        - password
        - category
      properties:
        email:
          type: string
          format: email
          example: "tecnico.comune@example.it"
        username:
          type: string
          example: "tcomune"
        companyName:
          type: string
          example: "Teodoro"
        password:
          type: string
          format: password
          example: "Str0ngP@ss!"
        category:
          $ref: '#/components/schemas/ReportCategory'

    CommentRequest:
      type: object
      required:
        - content
      properties:
        content:
          type: string
          example: "The issue has been scheduled for repair."

    Comment:
      type: object
      properties:
        id: 
          type: integer
          format: int64
          example: 1
        reportId:
          type: integer
          format: int64
          example: 1
        municipalityUserId:
          type: integer
          format: int64
          nullable: true
          example: 3
        externalMaintainerId:
          type: integer
          format: int64
          nullable: true
          example: 5
        content:
          type: string
          example: "The issue has been scheduled for repair."
        createdAt:
          type: string
          format: date-time
          example: "2025-11-05T10:15:00Z"
        updatedAt:
          type: string
          format: date-time
          example: "2025-11-05T10:15:00Z"
      description: >
        Internal comment on a report. Exactly one between municipalityUserId and
        externalMaintainerId is non-null (the author of the comment).

    Error:
      type: object
      required:
        - error
        - message
      properties:
        error:
          type: string
          description: Error type
          example: "Validation Error"
        message:
          type: string
          description: Detailed error message
          example: "Invalid input data"

  securitySchemes:
    cookieAuth:
      type: apiKey
      in: cookie
      name: authToken
      description: Authentication cookie containing session ID

tags:
  - name: Authentication
    description: Endpoints for user authentication
  - name: Users
    description: User registration and management
  - name: Reports
    description: Report management and submission

